stages:
  train:
    cmd: mkdir models && echo 1234 > models/best.ckpt
    deps:
    - data/raw
    outs:
    - models
  inference:
    cmd: mkdir predictions && echo 4321 > predictions/pred.txt
    deps:
    - data/raw
    - models
    outs:
    - predictions
